# 模块设计实现文档模板

> 本模板用于设计图驱动模块。**Phase A0 读图建档 + 四表 + 11 维 + 专业验收判定表未填完，禁止写代码。**

## 设计图读图建档（Phase A0 · 先于 token 表）

> 逐屏逐区读 `docs/design_assets/` 原图填写。格式见 `.cursor/skills/design-faithful-ui/SKILL.md`《设计图深度读图》A0-8。

### 屏 N：[design_assets 文件名]

- **主态：** （哪个 Tab/弹窗/选中项）
- **分区尺寸：** 侧栏 __px | 右栏 __px | …
- **本屏独有 vs 共用顶栏/侧栏：**

| 分区 | 子元素清单（来自设计图） | 计数 | 选中/激活态 |
|------|--------------------------|------|-------------|
| 顶栏 | | | |
| 左控制台 | | | |
| 画布 | | | |
| 右面板 | | | |

### 屏 N 文案抽取（逐字，不得改写）

| 位置 | 设计稿原文 |
|------|------------|
| | |

### 屏 N 实现核对点

- [ ] （例：测试态右栏为运行卡，非 Inspector）
- [ ] （例：画布 demo 5 节点 6 边）

---

## 模块信息

- 模块名称：
- 业务目标：
- 页面路由：
- 设计图来源：
- 本地设计图文件（`docs/design_assets/`）：

## 设计硬约束

- 哪些区域必须 1:1（默认：**全部可见区域**）：
- 哪些区域允许响应式调整（须写明断点与行为，默认：**无**，除非用户书面同意）：
- 是否允许保留旧页面壳（默认：**否**）：
- 设计稿主态是有数据还是空态：
- 数据条数与设计稿不同时，哪些**样式/密度**仍必须 1:1：

---

## 设计 token 表（实现前必填）

| token | 值 | 用于 |
|-------|-----|------|
| --color-bg-page | | 页面背景 |
| --color-bg-surface | | 卡片/面板背景 |
| --color-border | | 默认边框 |
| --color-primary | | 主按钮/链接 |
| --color-text-title | | 标题 |
| --color-text-body | | 正文 |
| --color-text-muted | | 辅助文字 |
| --font-family | | 全局字体 |
| --radius-sm / md / lg | | 圆角 |
| --shadow-card | | 卡片阴影 |
| --spacing-unit | | 基础间距刻度（如 4px） |

## 分区尺寸表（px，实现前必填）

| 区域 | 宽 | 高 | padding | 备注 |
|------|----|----|---------|------|
| 左栏 | | | | |
| 顶栏 | | | | |
| 主内容 | | | | |
| 右栏 | | | | |
| 底栏/日志 | | | | |

## 图标清单（实现前必填）

| 位置 | 设计描述 | 选用 icon | 来源/库 |
|------|----------|-----------|---------|
| | | | |

## 文案清单（与设计图逐字一致，实现前必填）

| 元素 selector/位置 | 设计稿文案 |
|--------------------|------------|
| | |

---

## 11 维 × 区块对照表（实现前必填）

| 区块 | 布局 | 间距 | 字体格式 | 字号 | 排版 | 颜色风格 | 图标 | 适配 | 视觉效果 | 反馈 | 文案 |
|------|------|------|----------|------|------|----------|------|------|----------|------|------|
| 侧栏 | | | | | | | | | | | |
| 顶栏 | | | | | | | | | | | |
| 工具条 | | | | | | | | | | | |
| 主内容 | | | | | | | | | | | |
| 右栏/Inspector | | | | | | | | | | | |
| 底栏/日志 | | | | | | | | | | | |
| 弹窗 | | | | | | | | | | | |

---

## 设计拆解清单（实现前必填）

- 页面外壳与宽度占比：
- 左侧导航结构、图标、激活态：
- 顶部栏 / 面包屑 / 标题区：
- 主按钮区尺寸、顺序、间距、图标：
- KPI / 摘要卡片：
- 筛选栏 / 搜索 / 视图切换：
- 批量操作栏：
- 主内容主态：
- 表格 / 卡片 / 图表细节：
- 分页器：
- 空态 / loading / error：
- hover / active / disabled / selected：

## 页面结构

- 顶部区域：
- 左侧 / 侧边区域：
- 中间主内容：
- 底部区域：

## 交互与操作

| 控件 | 真实? | 触发接口/逻辑 | 成功反馈 | 失败反馈 |
|------|-------|---------------|----------|----------|
| | | | | |

## 状态清单

- 默认态：
- hover：
- active：
- disabled：
- loading：
- empty：
- error：
- success：

## 数据与接口映射

- 页面接口：
- 字段映射：
- 前端展示转换规则：
- 后端缺口：

## 重写策略

- 是否重写旧模块（设计冲突默认 **是**）：
- 需要删除的旧逻辑：
- 需要保留的接口：
- 需要后补的能力：

## 响应式约束

- 目标基准分辨率（与设计图一致）：
- 宽屏行为：
- 中屏行为：
- 窄屏行为：
- 禁止出现的问题：overflow、盒中盒、按钮换行错位、字体缩小导致与设计稿比例失真

## 浏览器验收计划

- 目标本地地址：
- 需要核对的运行时 entrypoint / template / cache 版本：
- 需要逐区截图的区块列表：

## 专业验收判定表（交付前必填 — 全部 PASS 且结论为「通过验收」才可交付）

> 执行 `.cursor/skills/design-faithful-ui/SKILL.md` 中《专业验收机制》Step 1–6。验收员独立、不徇私、不业余。

| 区块 | 布局 | 间距 | 字体 | 颜色 | 图标 | 文案 | 交互反馈 | 结论 |
|------|------|------|------|------|------|------|----------|------|
| 侧栏 | | | | | | | | PASS / FAIL |
| 顶栏 | | | | | | | | |
| 工具条 | | | | | | | | |
| 主内容 | | | | | | | | |
| 右栏 | | | | | | | | |
| 底栏 | | | | | | | | |
| 弹窗 | | | | | | | | |

### 专业验收报告

- **验收结论（仅二选一）：** `通过验收` / `未通过验收`
- **对照设计图：** `[docs/design_assets/xxx.png]`
- **验收基准分辨率：**
- **实际页面 URL + cache 版本：**
- **FAIL 项（逐条：区块 — 设计 — 实际）：**
- **PASS 项摘要（可选，仅作记录）：**

**禁止写入：** 遗留、因为、除 XX 外、基本完成、可验收、后续再调。

## 验证清单

- [ ] JS 语法
- [ ] Python compile
- [ ] 服务 health
- [ ] cache 版本已 bump 且浏览器加载新版本
- [ ] 已执行专业验收 Step 1–6（并排截图取证）
- [ ] 专业验收判定表全部 PASS
- [ ] 专业验收报告结论为「通过验收」
- [ ] 正向交互路径
- [ ] 失败路径反馈
- [ ] 无乱码
