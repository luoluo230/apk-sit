# 项目管理设计落地映射

## 真源

- 产品与功能：`docs/design_specs/project_management_complete_product_design_prd.md`
- 视觉与交互：`docs/design_assets/project_management/`
- 基准视口：`1680 x 960`
- 运行入口：`/admin/projects/{project_id}/...`

## 第一阶段设计映射

| 编号 | 页面 | 设计图 | 实现状态 | 验收状态 |
|---|---|---|---|---|
| P02 | 项目总览 | `设计图/ChatGPT Image 2026年6月13日 10_54_38 (2).png` | 实施中 | 待浏览器验收 |
| P10 | 发布单中心 | 复用 P02/P12 列表与卡片语言 | 实施中 | 待浏览器验收 |
| P11 | 新建/编辑发布单 | `设计图/ChatGPT Image 2026年6月13日 10_54_40 (4).png` | 实施中 | 待浏览器验收 |
| P12 | 发布单详情 | `设计图/ChatGPT Image 2026年6月13日 10_54_44 (7).png` | 实施中 | 待浏览器验收 |

## 页面壳层硬规则

- 左侧导航固定 232px，顶部栏固定 72px。
- 顶部项目控件是真实上下文下拉，只切换项目和环境。
- 顶部栏禁止出现创建项目、创建拓扑等业务操作。
- 页面主操作位于页面标题区右侧。
- 项目页面跳转必须保留 `env_key/channel_id/platform/version_name/version_code/release_order_id`。
- 加载、空、失败、无权限、处理中必须有独立可见反馈。

## 第一阶段交互与后端映射

| 可见操作 | 后端行为 | 反馈 |
|---|---|---|
| 切换项目/环境 | 更新 URL 上下文并保持当前模块 | 页面重新加载并显示新上下文 |
| 创建发布单 | 进入 P11 独立页面 | 不使用弹窗 |
| 保存草稿 | 创建或更新 `release_orders` | Toast + 保留编辑态 |
| 保存并进入构建 | 保存发布单并触发构建 | 跳转 P12，显示构建状态 |
| 执行预检 | 真实检查产物、拓扑、runtime、网络参数 | 阻断/警告/通过分组展示 |
| 审批、发布、验证、回滚 | 调用发布单状态机动作 | 页面时间线与下一步动作刷新 |

## 设计稿对照差距清单（2026-06-25）

真源设计图目录：`docs/design_assets/project_management/`（已从 `D:\Art\设计图\设计图` 同步 21 张 PNG）。  
运行入口对照：`http://127.0.0.1:5003/admin/projects/{project_id}/...`

**整体结论（主观估算，非像素验收）：**

| 维度 | 与设计稿一致度 | 说明 |
|---|---|---|
| 全局壳层（侧栏/顶栏） | ~55% | 232px/72px 尺寸对，但导航分组、面包屑层级、顶栏能力不全 |
| 有设计稿的核心页（P02/P04/P06/P11/P12） | ~30–40% | 信息架构部分对齐，布局密度、右栏/抽屉、表格列、步骤器差距大 |
| PRD 全页面（P01–P27） | ~25% | 多数页面无独立设计稿或未按稿实现 |
| 图标与 Token | ~75% | `project_ui` 已接入设计包 SVG；圆角/阴影/字号层级未全站统一 |
| 交互与真实后端 | 功能 > 视觉 | 构建/发布/下载等链路在补，但 UI 未达设计稿工作流密度 |

### 全局壳层（所有项目页）

| 检查项 | 设计稿要求 | 当前实现 | 差异等级 |
|---|---|---|---|
| 侧栏宽度 | 232px | 232px（`ops_shell.css`） | 一致 |
| 顶栏高度 | 72px | 72px | 一致 |
| 侧栏分组 | 全局区 / 项目工作区（总览、交付、运行、治理、协作、设置多组） | 全局导航 / 项目空间 / 交付管理 / 运行管理 / 治理审计（5 组，无协作、无完整设置树） | 严重 |
| 导航项命名 | 「版本与 VersionCode」「构建与产物」「环境概览」等 | 「版本管理」「构建产物」；无独立环境概览导航 | 中等 |
| 面包屑 | 4 级：项目管理 / 项目工作区 / 模块 / 页面 | 3 级：项目管理 / 项目空间 / 页面标题 | 中等 |
| 顶栏项目切换 | 项目下拉 + 全局搜索 | 有项目下拉 + 搜索框（搜索未接全局检索 API） | 中等 |
| 顶栏环境/渠道/平台 | 设计在部分页面标题区或总览区 | 仅在页面内筛选，顶栏无环境切换 | 严重 |
| 收藏星标 | 页面标题旁星标 | 无 | 中等 |
| 页面主操作位置 | 标题区右侧（导出、新建等） | 部分页面有，不一致 | 中等 |
| 右侧辅助栏 | 280px 成员/帮助/快捷入口 | 仅部分页面有右栏，宽度与内容不一致 | 严重 |
| 字体家族 | Inter + PingFang SC | 已引用 `design-tokens.css` | 接近 |
| 页面背景 | #F6F8FC | 渐变浅蓝 `#f8faff` 系 | 轻微 |
| 卡片圆角 | 16px | 多为 8–10px | 中等 |
| 卡片阴影 | `--shadow-sm` | 混用多种 shadow，build 页内联样式 | 中等 |
| 主色 | #1677FF | #1677FF（部分旧页 `#2563eb`） | 轻微 |

### P02 项目总览（设计图 `10_54_38 (2).png`）

| 检查项 | 设计稿 | 当前 | 差异等级 |
|---|---|---|---|
| 页面结构 | 标题 + 四维筛选（项目/环境/渠道/平台）+ 更新时间 | Tab（总览/交付/渠道）+ 部分筛选 | 严重 |
| KPI 区 | 5 卡：当前版本/服务健康/今日构建/待处理变更/成员，带趋势箭头 | KPI 存在但指标、图标底纹、趋势样式不同 | 严重 |
| 环境运行概览 | 4 环境卡片横排，内含健康度/在线实例/Agent/更新时间 | 环境卡片有，字段与视觉密度不足 | 中等 |
| 最近活动 | 多 Tab（全部/发布/构建/变更/告警…）+ 头像时间线 | 活动流简化或缺失 Tab | 严重 |
| 右侧栏 | 成员列表 + 帮助支持 4 项 + 快捷入口 4 图标 | 快捷入口部分有；帮助区、成员卡片样式不符 | 严重 |
| 主操作 | 收藏、筛选联动 | 无收藏 | 中等 |

### P04 版本与 VersionCode（设计图 `10_54_39 (3).png`）

| 检查项 | 设计稿 | 当前 | 差异等级 |
|---|---|---|---|
| 标题区操作 | 导出 / 新建版本组 / 新增 VersionCode | 构建与产物 / 新建版本组 | 中等 |
| KPI | 5 卡：版本组数、有效 VC、构建中、产物完整、已发布 | KPI 有，指标与彩色图标底纹不一致 | 严重 |
| 筛选 | 环境/渠道/平台/版本状态/产物状态 + 关键词 + 重置 | 环境/渠道/平台/状态 + 搜索，无产物状态 | 中等 |
| 主列表 | 标准表格 + 可展开版本组 + 10 列 | 自定义 group/child 行，列数少（约 6 列） | 严重 |
| 版本名称标签 | Official/Pre-release/Test 等彩色 Tag | 版本组 pill，无名称 Tag 体系 | 严重 |
| 行操作 | 查看产物/创建发布单/重新构建/查看日志 | 发布单/构建/历史；下载弹窗已加 | 中等 |
| 右侧抽屉 | 固定 360px+ VC 详情（构建进度条、产物完整性、发布单、Bundle） | 无右侧抽屉，仅下载 Dialog | 严重 |
| 分页 | 底部分页器 | 无分页（全量渲染） | 中等 |

### P06 构建与产物（设计图 `10_54_41 (5).png`）

| 检查项 | 设计稿 | 当前 | 差异等级 |
|---|---|---|---|
| KPI | 今日构建/成功率/产物总数/缺失产物/待清理 | 全部构建/成功/进行中/失败/VersionCode 数 | 严重 |
| 筛选 | 状态/环境/平台/产物类型 + 日期范围 | 状态/平台/搜索；无产物类型、无日期 | 严重 |
| 构建表列 | Job/Task、产物数、触发人等 | 缺 Job 列；列结构与图标不同 | 中等 |
| 产物列表区 | 底部 Tab：最新产物/按构建/按类型 + SHA256 表 | 无独立产物列表区块 | 严重 |
| 右侧栏 | 当前构建详情 + 失败摘要 + 快捷操作（重试等） | 有右侧栏，内容与按钮集合不全 | 中等 |
| 范围过滤 | 设计为全局项目构建视图 | 已支持 URL 范围过滤（VC/环境） | 实现增强（设计未显式） |

### P11 新建发布单（设计图 `10_54_40 (4).png`）

| 检查项 | 设计稿 | 当前 | 差异等级 |
|---|---|---|---|
| 布局 | 左 6 步步骤条 + 中大表单 + 右风险提示/指引/模板 | 单页表单，无步骤条、无右侧栏 | 严重 |
| 表单分组 | 交付目标/构建计划/拓扑/发布策略/验证/回滚 六大块 | 字段部分覆盖，分组与密度远少于设计 | 严重 |
| 生产环境警示 | 顶部橙色 Banner | 无或弱化 | 中等 |
| 底部操作 | 取消/保存草稿/保存并进入构建/复制计划 + 自动保存提示 | 部分保存按钮，无步骤流转 | 严重 |

### P12 发布单详情（设计图 `10_54_44 (7).png`）

| 检查项 | 设计稿 | 当前 | 差异等级 |
|---|---|---|---|
| 顶部 | 状态 Badge + 单号复制 + 回滚/执行验证/查看报告 | 简化头部，操作不全 | 严重 |
| 横向步骤器 | 8 步（计划→构建→拓扑→预检→审批→发布→验证→Bundle） | 无完整横向步骤 | 严重 |
| 阻断/警告/下一步 | 三列彩色卡片 | 无 | 严重 |
| 详情网格 | 6 卡：构建产物/拓扑Runtime/审批/发布执行/验证结果/Bundle回滚 | 部分信息散落，非 6 卡布局 | 严重 |
| 事件时间线 | 右侧纵向时间线 | 无独立时间线栏 | 严重 |

### 图标与字体（全站）

| 检查项 | 设计稿 | 当前 | 差异等级 |
|---|---|---|---|
| 导航/操作图标 | 65 个 SVG，24px 线型 | 已复制至 `static/project_ui/svg/`，侧栏在用 | 接近 |
| KPI 图标 | 彩色实心圆底图标 | 部分页面用文字/简单色块 | 中等 |
| 平台图标 | Android/iOS 文件图标 | 部分用文本「Android」 | 中等 |
| H1 页面标题 | 24px / 600 | 约 19–24px 混用 | 轻微 |
| Body | 14px / 22 | 12–14px 混用 | 轻微 |
| Caption/标签 | 12px | 11–12px，基本接近 | 接近 |
| 状态色 | Success #52C41A 等 | 部分页用 Tailwind 色 (#169b51 等) | 轻微 |

### PRD 页面覆盖（无/缺设计稿）

| 页面编号 | 页面 | 设计稿 | 实现概况 |
|---|---|---|---|
| P01 | 项目列表 | [`P01_project_list_10_54_37.png`](../design_assets/project_management/P01_project_list_10_54_37.png) | [`project_list_p01.md`](project_list_p01.md) — pm13 组件重构，屏级 PASS 表进行中 |
| P03 | 环境详情 | 可能在其他 PNG | 有 `project_environment_detail`，未对照验收 |
| P05 | VersionCode 详情 | 设计在 P04 右栏 | 无独立详情页，缺右栏 |
| P08 | 下载中心 | 无 | 未独立实现 |
| P09 | 测试设备 | 无 | 未实现 |
| P10 | 发布单中心 | 复用列表语言 | 有列表，视觉未对齐 P02 |
| P16 | 环境运行工作台 | `P16_environment_runtime_overview.png` | `/environments/{env}/runtime` MVP-A 已立项 |

### 拓扑绑定（P18 整合 · 2026-07-20）

规格：[`topology_binding_architecture.md`](topology_binding_architecture.md) · 环境详情：[`project_environment_detail_p18.md`](project_environment_detail_p18.md)

| 检查项 | 目标 | 2026-07-20 实现 | 差异 |
|---|---|---|---|
| 绑定入口 | 仅交付线卡片抽屉 | 卡片服务端「拓扑」+ 抽屉；侧栏/右栏已移除 | ✅ |
| 拓扑选择 | 全量列表+详情 | picker API + 抽屉卡片列表 | ✅ |
| platform 维度 | Android/iOS 独立绑定 | `topology_bindings.platform` + resolver | ✅ |
| 解析链 | 单一 `resolve_topology_binding_for_scope` | 交付线/发布/运行概览已接入 | ✅ |
| 独立绑定页 | 兼容深链 redirect | `/topology-bindings` → 环境详情抽屉 | ✅ |
| 卡片尺寸 | 260–320px + 页签 | `project_environment_detail.css` | ✅ |
| bundle 引用提示 | 抽屉内 bundle_ref_count | 占位 0，待 P3 | 轻微 |
| 屏级像素验收 | 对照设计资产 | 待浏览器 PASS 表 | 待验收 |


1. **壳层统一**：补全侧栏 IA、四级面包屑、页面标题区规范（星标/主操作/筛选位）。  
2. **P04 + P06**：表格化 + 右栏/抽屉 + KPI/筛选对齐设计稿（交付链路最高频）。  
3. **P11 + P12**：发布单创建/详情按步骤器与工作流卡片重写。  
4. **P02**：环境卡 + 活动流 + 右栏三块对齐。  
5. **全站 Token 清扫**：圆角 16px、阴影、字号层级统一到 `design-tokens.css`。

## 实施进度（2026-06-25 计划落地）

| 阶段 | 状态 | 关键交付 |
|------|------|----------|
| 阶段0 | 完成 | `project_ui/pm-*.css`、四级面包屑、侧栏 IA、星标/顶栏搜索 |
| 阶段1 P04 | 完成 | VC 抽屉 D02、KPI/筛选/分页/导出、`v=20260625-pm7` |
| 阶段1 P06 | 完成 | `project_build_history.css/js`、产物 Tab、scoped banner 保留 |
| 阶段1 P11/P12 | 完成 | 纵向/横向步骤器、三列告警、生产 Banner |
| 阶段1 P02/P10 | 完成 | pm-kpi 卡片、发布单列表 pm 样式 |
| 阶段2 | 完成 | 下载中心、测试设备、设置、审计跳转 |
| 阶段3–5 | 完成 | 运行/治理 breadcrumb、pm-modal 弹窗类 |

回归：`test_delivery_scope` + `test_scope_resolver` 6 passed。屏级 PASS 表待浏览器逐页填写。

第一阶段完成后必须逐页记录：

- 设计图与运行截图路径
- 壳层、标题区、主内容、右侧栏、文案、图标、交互的 PASS/FAIL
- 浏览器实际点击过的操作
- 实际加载的 CSS/JS cache 版本

## 全页面与弹窗截图门禁

- 必须逐页截图：项目总览、版本与 VersionCode、构建与产物、发布单中心、新建/编辑发布单、发布单详情、拓扑资产、拓扑绑定、拓扑画布、Agent 与服务器、运行执行、节点诊断、变更治理。
- 必须逐个截图所有可见弹窗、抽屉、确认框、下拉菜单、空状态、加载状态、失败状态与高风险状态。
- 有设计图的页面逐项对比布局、间距、字体、颜色、图标、边框、阴影、状态和文案；任何可见差异均为 FAIL。
- 无独立设计图的页面必须使用统一项目壳层、Token、组件、真实图片/SVG 图标和一致交互反馈；出现旧壳或第二套视觉语言即为 FAIL。
- 任何图标槽位禁止使用字符、Emoji、字体图标或文字模拟；优先使用设计师图标，缺失时补同风格 SVG。
- 每个截图验收必须同时验证页面可见操作具备真实后端效果，不允许只截图静态壳层。

## 最终生产验收硬门禁

以下三项必须同时通过，任意一项失败即视为项目管理模块未完成：

1. UI 无错乱：已有设计图页面严格按设计图还原，缺图页面严格遵循同一设计系统；无挤压、重叠、截断、旧壳残留和布局漂移。
2. 功能全部真实可用：每个可见按钮、筛选、标签、弹窗、抽屉和跳转均有真实业务效果；复杂流程使用独立页面，不错误嵌入其他模块；不存在装饰性假功能。
3. 全链路生产可用：版本、构建、产物、拓扑、runtime、预检、审批、发布、客户端 bootstrap、热更、登录、玩家数据、验证和回滚全部使用真实数据与真实服务打通；不存在只完成 UI 壳或返回假成功的环节。
