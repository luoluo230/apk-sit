[pytest]
testpaths = tests
python_files = test_*.py
markers =
    fast: unit/contract tests without live GameServer dependencies
    live: requires running GameServer, Mongo, Redis, or admin server
    windows: Windows-specific behavior (tasklist, process launch)
addopts = -ra
